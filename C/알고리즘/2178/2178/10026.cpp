#include <stdio.h>

int main(void)
{
	int list[100][100];
	int n, m;

	scanf("%d %d", &n, &m);

	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j < m; j++)
		{
			scanf("%d", list[i][j]);
		}
	}

	for(int i=0; i<n)
}